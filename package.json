{
  "name": "supersent-website",
  "version": "1.3.0",
  "private": true,
  "description": "SuperSent macOS 与 Windows 官方产品网站",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build",
    "serve": "node scripts/serve.mjs",
    "deploy": "npm run check && wrangler pages deploy dist --project-name supersent-website --branch main"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
